# Harness compatibility instructions

The project uses small, isolated fixtures. Prefer the existing source layout and avoid adding dependencies.

## Completion

A request is complete only after the fixture's requested verification has actually passed.
